From 208d153d747e88b1ddf9a09d29fe8c7a77cc45ef Mon Sep 17 00:00:00 2001
From: Lichtenshtein <altruumann@protonmail.com>
Date: Sun, 26 Apr 2026 20:24:01 +0300
Subject: [PATCH] fix dynamic lib installation

---
 src_base/CMakeLists.txt | 124 ++++++++-----------------------
 src_main/CMakeLists.txt | 159 +++++++++-------------------------------
 2 files changed, 65 insertions(+), 218 deletions(-)

diff --git a/src_base/CMakeLists.txt b/src_base/CMakeLists.txt
index 8683f40..18dd68b 100644
--- a/src_base/CMakeLists.txt
+++ b/src_base/CMakeLists.txt
@@ -6,142 +6,78 @@ file (GLOB LIB_BASE_SRC "xevd_*.c")
 file (GLOB LIB_BASE_INC "xevd_*.h" )
 file (GLOB LIB_SSE_SRC "sse/xevd_*.c")
 file (GLOB LIB_SSE_INC "sse/xevd_*.h" )
-file (GLOB LIB_AVX_SRC "../src_base/avx/xevd_*.c")
-file (GLOB LIB_AVX_INC "../src_base/avx/xevd_*.h" )
+file (GLOB LIB_AVX_SRC "avx/xevd_*.c")
+file (GLOB LIB_AVX_INC "avx/xevd_*.h" )
 file (GLOB LIB_NEON_SRC "neon/xevd_*.c")
 file (GLOB LIB_NEON_INC "neon/xevd_*.h" )
 
 include(GenerateExportHeader)
 include_directories("${CMAKE_CURRENT_BINARY_DIR}")
 
-add_library(${LIB_NAME_BASE} STATIC ${LIB_API_SRC} ${XEVD_INC} ${LIB_BASE_SRC} ${LIB_BASE_INC})
+set(BASE_SOURCES ${LIB_API_SRC} ${XEVD_INC} ${LIB_BASE_SRC} ${LIB_BASE_INC})
 
-add_library(${LIB_NAME}_shared SHARED
-        ${LIB_API_MAIN_SRC}
-        ${XEVD_INC}
-        ${LIB_BASE_SRC}
-        ${LIB_BASE_INC}
-        ${LIB_MAIN_SRC}
-        ${LIB_MAIN_INC})
+add_library(${LIB_NAME_BASE} STATIC ${BASE_SOURCES})
+add_library(${LIB_NAME_BASE}_shared SHARED ${BASE_SOURCES})
 
-set_target_properties(${LIB_NAME}_shared PROPERTIES OUTPUT_NAME ${LIB_NAME})
+set_target_properties(${LIB_NAME_BASE} PROPERTIES OUTPUT_NAME ${LIB_NAME_BASE})
+set_target_properties(${LIB_NAME_BASE}_shared PROPERTIES OUTPUT_NAME ${LIB_NAME_BASE})
 
 add_library(XEVD::${LIB_NAME_BASE} ALIAS ${LIB_NAME_BASE})
 
 if("${ARM}" STREQUAL "TRUE")
-    target_sources(${LIB_NAME_BASE} PRIVATE ${LIB_NEON_SRC} ${LIB_NEON_INC})
+    set(SIMD_SOURCES ${LIB_NEON_SRC} ${LIB_NEON_INC})
 else()
-    target_sources(${LIB_NAME_BASE} PRIVATE ${LIB_SSE_SRC} ${LIB_SSE_INC} ${LIB_AVX_SRC} ${LIB_AVX_INC})
+    set(SIMD_SOURCES ${LIB_SSE_SRC} ${LIB_SSE_INC} ${LIB_AVX_SRC} ${LIB_AVX_INC})
 endif()
 
+target_sources(${LIB_NAME_BASE} PRIVATE ${SIMD_SOURCES})
+target_sources(${LIB_NAME_BASE}_shared PRIVATE ${SIMD_SOURCES})
+
 if(NOT MSVC AND CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64|AMD64")
-    target_compile_options(${LIB_NAME_BASE} PRIVATE
-            -mssse3
-            -msse4.1
-            -msse4.2
-            -mavx
-            -mavx2
-            -fno-tree-vectorize
-    )
+    set(X86_FLAGS -mssse3 -msse4.1 -msse4.2 -mavx -mavx2 -fno-tree-vectorize)
+    target_compile_options(${LIB_NAME_BASE} PRIVATE ${X86_FLAGS})
+    target_compile_options(${LIB_NAME_BASE}_shared PRIVATE ${X86_FLAGS})
 endif ()
 
-source_group("base\\header" FILES ${LIB_BASE_INC} ${XEVD_INC})
-source_group("base\\source" FILES ${LIB_BASE_SRC} ${LIB_API_SRC})
-source_group("base\\sse\\header" FILES ${LIB_SSE_INC})
-source_group("base\\sse\\source" FILES ${LIB_SSE_SRC})
-source_group("base\\avx\\header" FILES ${LIB_AVX_INC})
-source_group("base\\avx\\source" FILES ${LIB_AVX_SRC})
-source_group("base\\neon\\header" FILES ${LIB_NEON_INC})
-source_group("base\\neon\\source" FILES ${LIB_NEON_SRC})
-
-target_include_directories(${LIB_NAME_BASE} PUBLIC
+set(BASE_INCLUDE_DIRS
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../inc>
     $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
 )
 
-if("${ARM}" STREQUAL "TRUE")
-    include_directories( ${LIB_NAME_BASE} PUBLIC ./neon)
-else()
-    include_directories( ${LIB_NAME_BASE} PUBLIC ./sse ./avx)
-endif()
-
-
-set_target_properties(${LIB_NAME_BASE} PROPERTIES FOLDER lib
-                                                  ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/lib)
-
-
-set( SSE ${BASE_INC_FILES} ${LIB_SSE_SRC})
-set( AVX ${LIB_AVX_SRC} )
-set( NEON ${LIB_NEON_SRC})
-
-
-if( MSVC )
-    target_compile_definitions( ${LIB_NAME_BASE} PUBLIC ANY _CRT_SECURE_NO_WARNINGS )
+target_include_directories(${LIB_NAME_BASE} PUBLIC ${BASE_INCLUDE_DIRS})
+target_include_directories(${LIB_NAME_BASE}_shared PUBLIC ${BASE_INCLUDE_DIRS})
 
-    # Since both the import library associated with DLL and the static library have the same names
-    # they must be build in different locations to avoid overwriting.
-    #
-    set_target_properties(${LIB_NAME_BASE} PROPERTIES FOLDER lib
-                            ARCHIVE_OUTPUT_DIRECTORY  ${CMAKE_CURRENT_BINARY_DIR}/static/lib)
-
-elseif( UNIX OR MINGW )
-
-    if("${ARM}" STREQUAL "FALSE")
-        set_property( SOURCE ${SSE} APPEND PROPERTY COMPILE_FLAGS "-msse4.1" )
-        set_property( SOURCE ${AVX} APPEND PROPERTY COMPILE_FLAGS " -mavx2" )
-    endif()
-    target_compile_definitions( ${LIB_NAME_BASE} PUBLIC ANY LINUX )
+if(UNIX OR MINGW)
+    target_compile_definitions(${LIB_NAME_BASE} PUBLIC ANY LINUX)
+    target_compile_definitions(${LIB_NAME_BASE}_shared PUBLIC ANY LINUX)
     target_link_libraries(${LIB_NAME_BASE} m)
+    target_link_libraries(${LIB_NAME_BASE}_shared m)
 endif()
 
-# Install rules
-#
-# Set DCMAKE_INSTALL_PREFIX to change default install prefix
-# e.g cmake .. -DSET_PROF=BASE -DCMAKE_INSTALL_PREFIX='C:\Users\Samsung\git\xevd_internal\build-windows\install'
-
-# List the headers we want to declare as public for installation.
-set(XEVD_PUBLIC_HEADERS "${XEVD_INC}")
-set_target_properties(${LIB_NAME_BASE} PROPERTIES PUBLIC_HEADER "${XEVD_PUBLIC_HEADERS}")
-
-set(XEVD_PRIVATE_HEADERS "${LIB_BASE_INC}" "${LIB_SSE_INC}" "${LIB_AVX_INC}")
-
 if (XEVD_INSTALL)
-
     include(GNUInstallDirs)
 
-    # Install library and public headers
-    install(TARGETS ${LIB_NAME_BASE}
-            EXPORT XEVDTargets
-            ARCHIVE COMPONENT Development DESTINATION ${CMAKE_INSTALL_LIBDIR}
-            LIBRARY COMPONENT Libraries DESTINATION ${CMAKE_INSTALL_LIBDIR}
-            RUNTIME COMPONENT Libraries DESTINATION ${CMAKE_INSTALL_BINDIR}
-            PUBLIC_HEADER COMPONENT Development DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
-            INCLUDES DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
-    )
+    set_target_properties(${LIB_NAME_BASE} PROPERTIES PUBLIC_HEADER "${XEVD_INC}")
 
-    install(TARGETS ${LIB_NAME} ${LIB_NAME}_shared
+    install(TARGETS ${LIB_NAME_BASE} ${LIB_NAME_BASE}_shared
             EXPORT XEVDTargets
             ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
             LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
             RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+            PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
     )
 
     configure_file(
-        "${CMAKE_CURRENT_SOURCE_DIR}/pkgconfig/${LIB_NAME_BASE}.pc.in"
+        "${CMAKE_CURRENT_SOURCE_DIR}/../pkgconfig/${LIB_NAME_BASE}.pc.in"
         "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME_BASE}.pc"
-        IMMEDIATE @ONLY)
+        @ONLY)
 
-    install( FILES
-        "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME_BASE}.pc" COMPONENT Development DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig
-    )
+    install(FILES "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME_BASE}.pc"
+            DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
-    # Install the export target
     install(EXPORT XEVDTargets
             FILE XEVDTargets.cmake
             NAMESPACE XEVD::
-            DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/XEVD
-            COMPONENT Development
-    )
-
+            DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/XEVD)
 endif ()
diff --git a/src_main/CMakeLists.txt b/src_main/CMakeLists.txt
index d37582c..1241651 100644
--- a/src_main/CMakeLists.txt
+++ b/src_main/CMakeLists.txt
@@ -22,158 +22,69 @@ file (GLOB LIB_MAIN_NEON_INC "./neon/xevdm_*.h" )
 include(GenerateExportHeader)
 include_directories("${CMAKE_CURRENT_BINARY_DIR}")
 
-add_library(${LIB_NAME} STATIC
-        ${LIB_API_MAIN_SRC}
-        ${XEVD_INC}
-        ${LIB_BASE_SRC}
-        ${LIB_BASE_INC}
-        ${LIB_MAIN_SRC}
-        ${LIB_MAIN_INC})
-
-add_library(${LIB_NAME}_shared SHARED
-        ${LIB_API_MAIN_SRC}
-        ${XEVD_INC}
-        ${LIB_BASE_SRC}
-        ${LIB_BASE_INC}
-        ${LIB_MAIN_SRC}
-        ${LIB_MAIN_INC})
+set(ALL_SOURCES ${LIB_API_MAIN_SRC} ${XEVD_INC} ${LIB_BASE_SRC} ${LIB_BASE_INC} ${LIB_MAIN_SRC} ${LIB_MAIN_INC})
 
-set_target_properties(${LIB_NAME}_shared PROPERTIES OUTPUT_NAME ${LIB_NAME})
-
-add_library(XEVD::${LIB_NAME} ALIAS ${LIB_NAME})
+add_library(${LIB_NAME} STATIC ${ALL_SOURCES})
+add_library(${LIB_NAME}_shared SHARED ${ALL_SOURCES})
 
-if("${ARM}" STREQUAL "TRUE")
-    target_sources(${LIB_NAME} PRIVATE
-            ${LIB_NEON_SRC}
-            ${LIB_NEON_INC}
-            ${LIB_MAIN_NEON_SRC}
-            ${LIB_MAIN_NEON_INC})
-else()
-    target_sources(${LIB_NAME} PRIVATE
-            ${LIB_SSE_SRC}
-            ${LIB_SSE_INC}
-            ${LIB_MAIN_SSE_SRC}
-            ${LIB_MAIN_SSE_INC}
-            ${LIB_AVX_SRC}
-            ${LIB_AVX_INC}
-            ${LIB_MAIN_AVX_SRC}
-            ${LIB_MAIN_AVX_INC})
-endif()
-
-if(NOT MSVC AND CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64|AMD64")
-    target_compile_options(${LIB_NAME} PRIVATE
-            -mssse3
-            -msse4.1
-            -msse4.2
-            -mavx
-            -mavx2
-            -fno-tree-vectorize
-    )
-endif ()
-
-source_group("base\\header" FILES ${LIB_BASE_INC} ${XEVD_INC})
-source_group("base\\source" FILES ${LIB_BASE_SRC} ${LIB_API_SRC})
-source_group("base\\sse\\header" FILES ${LIB_SSE_INC})
-source_group("base\\sse\\source" FILES ${LIB_SSE_SRC})
-source_group("base\\neon\\header" FILES ${LIB_NEON_INC})
-source_group("base\\neon\\source" FILES ${LIB_NEON_SRC})
-source_group("base\\avx\\header" FILES ${LIB_AVX_INC})
-source_group("base\\avx\\source" FILES ${LIB_AVX_SRC})
-source_group("main\\header" FILES ${LIB_MAIN_INC})
-source_group("main\\source" FILES ${LIB_MAIN_SRC} ${LIB_API_MAIN_SRC})
-source_group("main\\sse\\header" FILES ${LIB_MAIN_SSE_INC})
-source_group("main\\sse\\source" FILES ${LIB_MAIN_SSE_SRC})
-source_group("main\\avx\\header" FILES ${LIB_MAIN_AVX_INC})
-source_group("main\\avx\\source" FILES ${LIB_MAIN_AVX_SRC})
-source_group("main\\neon\\header" FILES ${LIB_MAIN_NEON_INC})
-source_group("main\\neon\\source" FILES ${LIB_MAIN_NEON_SRC})
-
-target_include_directories(${LIB_NAME} PUBLIC
-        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
-        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../src_base>
-        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../inc>
-        $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
-)
+set_target_properties(${LIB_NAME} PROPERTIES OUTPUT_NAME ${LIB_NAME})
+set_target_properties(${LIB_NAME}_shared PROPERTIES OUTPUT_NAME ${LIB_NAME})
 
 if("${ARM}" STREQUAL "TRUE")
-    include_directories( ${LIB_NAME} PUBLIC ./neon ../src_base/neon)
+    set(SIMD_SOURCES ${LIB_NEON_SRC} ${LIB_NEON_INC} ${LIB_MAIN_NEON_SRC} ${LIB_MAIN_NEON_INC})
 else()
-    include_directories( ${LIB_NAME} PUBLIC ./sse ./avx ../src_base/sse ../src_base/avx)
+    set(SIMD_SOURCES ${LIB_SSE_SRC} ${LIB_SSE_INC} ${LIB_MAIN_SSE_SRC} ${LIB_MAIN_SSE_INC} ${LIB_AVX_SRC} ${LIB_AVX_INC} ${LIB_MAIN_AVX_SRC} ${LIB_MAIN_AVX_INC})
 endif()
 
-set( SSE ${BASE_INC_FILES} ${LIB_SSE_SRC} ${LIB_MAIN_SSE_SRC})
-set( AVX ${LIB_AVX_SRC} ${LIB_MAIN_AVX_SRC})
-
-set_target_properties(${LIB_NAME} PROPERTIES OUTPUT_NAME ${LIB_NAME})
+target_sources(${LIB_NAME} PRIVATE ${SIMD_SOURCES})
+target_sources(${LIB_NAME}_shared PRIVATE ${SIMD_SOURCES})
 
-if( MSVC )
-    target_compile_definitions( ${LIB_NAME} PUBLIC ANY _CRT_SECURE_NO_WARNINGS )
+set(XEVD_INCLUDE_DIRS
+    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
+    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../src_base>
+    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../inc>
+    $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
+)
 
-    # Since both the import library associated with DLL and the static library have the same names
-    # they must be build in different locations to avoid overwriting.
-    #
-    set_target_properties(${LIB_NAME} PROPERTIES FOLDER lib
-                            ARCHIVE_OUTPUT_DIRECTORY  ${CMAKE_CURRENT_BINARY_DIR}/static/lib)
+target_include_directories(${LIB_NAME} PUBLIC ${XEVD_INCLUDE_DIRS})
+target_include_directories(${LIB_NAME}_shared PUBLIC ${XEVD_INCLUDE_DIRS})
 
-elseif( UNIX OR MINGW )
-    if("${ARM}" STREQUAL "FALSE")
-        set_property( SOURCE ${SSE} APPEND PROPERTY COMPILE_FLAGS "-msse4.1" )
-        set_property( SOURCE ${AVX} APPEND PROPERTY COMPILE_FLAGS " -mavx2" )
-    endif()
+if(NOT MSVC AND CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64|AMD64")
+    set(X86_FLAGS -mssse3 -msse4.1 -msse4.2 -mavx -mavx2 -fno-tree-vectorize)
+    target_compile_options(${LIB_NAME} PRIVATE ${X86_FLAGS})
+    target_compile_options(${LIB_NAME}_shared PRIVATE ${X86_FLAGS})
+endif()
 
-    set_target_properties(${LIB_NAME}_dynamic PROPERTIES FOLDER lib LIBRARY_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/lib)
-    target_link_libraries(${LIB_NAME}_dynamic m)
-    target_compile_definitions( ${LIB_NAME} PUBLIC ANY LINUX )
+if(UNIX OR MINGW)
+    target_compile_definitions(${LIB_NAME} PUBLIC ANY LINUX)
+    target_compile_definitions(${LIB_NAME}_shared PUBLIC ANY LINUX)
     target_link_libraries(${LIB_NAME} m)
+    target_link_libraries(${LIB_NAME}_shared m)
 endif()
 
-# Install rules
-#
-# Set DCMAKE_INSTALL_PREFIX to change default install prefix
-# e.g cmake .. -DCMAKE_INSTALL_PREFIX='C:\Users\Samsung\git\xevd_internal\build-windows\install'
-
-# List the headers we want to declare as public for installation.
-set(XEVD_PUBLIC_HEADERS "${XEVD_INC}")
-set_target_properties(${LIB_NAME} PROPERTIES PUBLIC_HEADER "${XEVD_PUBLIC_HEADERS}")
-
-set(XEVD_PRIVATE_HEADERS "${LIB_BASE_INC}" "${LIB_SSE_INC}" "${LIB_AVX_INC}")
-
-if (XEVD_INSTALL)
-
+if(XEVD_INSTALL)
     include(GNUInstallDirs)
 
-    # Install library and public headers
-    install(TARGETS ${LIB_NAME}
-            EXPORT XEVDTargets
-            ARCHIVE COMPONENT Development DESTINATION ${CMAKE_INSTALL_LIBDIR}
-            LIBRARY COMPONENT Libraries DESTINATION ${CMAKE_INSTALL_LIBDIR}
-            RUNTIME COMPONENT Libraries DESTINATION ${CMAKE_INSTALL_BINDIR}
-            PUBLIC_HEADER COMPONENT Development DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
-            INCLUDES DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
-    )
+    set_target_properties(${LIB_NAME} PROPERTIES PUBLIC_HEADER "${XEVD_INC}")
 
     install(TARGETS ${LIB_NAME} ${LIB_NAME}_shared
             EXPORT XEVDTargets
             ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
             LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
             RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+            PUBLIC_HEADER DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
     )
 
     configure_file(
-        "${CMAKE_CURRENT_SOURCE_DIR}/pkgconfig/${LIB_NAME}.pc.in"
+        "${CMAKE_CURRENT_SOURCE_DIR}/../pkgconfig/${LIB_NAME}.pc.in"
         "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME}.pc"
-        IMMEDIATE @ONLY)
+        @ONLY)
 
-    install( FILES
-        "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME}.pc" COMPONENT Development DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig
-    )
+    install(FILES "${CMAKE_CURRENT_BINARY_DIR}/${LIB_NAME}.pc"
+            DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
 
-    # Install the export target
     install(EXPORT XEVDTargets
             FILE XEVDTargets.cmake
             NAMESPACE XEVD::
-            DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/XEVD
-            COMPONENT Development
-    )
-
-endif ()
+            DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/XEVD)
+endif()
-- 
2.52.0.windows.1

